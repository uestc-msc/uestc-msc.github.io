language: node_js
branches:
  only:
  - Root
before_install:
  - nvm install stable
  - npm install -g hexo-cli
  - npm install
cache:
  directories:
    - node_modules
script:
  - hexo clean
  - hexo d -g